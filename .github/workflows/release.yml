name: Build and Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.platform }} ${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            platform: macOS
            arch: arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install frontend dependencies
        run: npm -C frontend install

      - name: Install Wails CLI
        run: |
          go install github.com/wailsapp/wails/v2/cmd/wails@v2.11.0
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Build GUI app
        run: wails build -clean

      - name: Build CLI binary
        run: go build -ldflags="-s -w" -o build/bin/downleaf ./cmd/downleaf

      - name: Remove quarantine attribute
        run: xattr -cr build/bin/downleaf.app

      - name: Package artifact
        run: |
          cd build/bin
          zip -r "../../downleaf-${{ matrix.platform }}-${{ matrix.arch }}-${{ github.ref_name }}.zip" .

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: downleaf-${{ matrix.platform }}-${{ matrix.arch }}-${{ github.ref_name }}
          path: downleaf-${{ matrix.platform }}-${{ matrix.arch }}-${{ github.ref_name }}.zip

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          files: artifacts/**/*.zip
